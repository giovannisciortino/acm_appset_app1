apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app1-noprod4-service2
  namespace: openshift-gitops
  finalizers:
  - resources-finalizer.argocd.argoproj.io # Cancella i pod se cancelli la App
spec:
  project: default
  source:
    repoURL: 'https://github.com/giovannisciortino/acm_appset_app1.git'
    targetRevision: noprod4
    path: workloads/service2  # Punta al deployment reale
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: app1-noprod4   # Il namespace di destinazione richiesto
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true    # Crea il namespace app1-noprod4 se non esiste
